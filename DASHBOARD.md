# 보안 분석 대시보드 설명

이 문서는 Vision CAPTCHA 관리자 대시보드에서 보여주는 지표와 해석 기준을 정리합니다.

## 기본 집계 기준

- 대시보드는 `GET /api/dashboard/security` API를 사용합니다.
- 기본 집계 대상은 최근 24시간 동안 저장된 `security_events` 데이터입니다.
- API는 최신순으로 최대 200건을 조회해 화면 지표를 계산합니다.
- CAPTCHA 통과/실패 판정이 서비스의 주 탐지 기준입니다.
- 행동패턴 데이터는 봇 확정 판정이 아니라 관리자 참고용 경고 정보입니다.

## 상단 요약 카드

### 오늘 보안 이벤트

최근 24시간 동안 기록된 전체 보안 이벤트 수입니다.

대상 이벤트는 CAPTCHA 판정 이벤트와 로그인 시도 이벤트를 포함합니다.

### CAPTCHA 통과율

최근 24시간 CAPTCHA 이벤트 중 `success`로 기록된 비율입니다.

계산식:

```text
CAPTCHA 통과율 = 성공 CAPTCHA 수 / 전체 CAPTCHA 수 * 100
```

Level 1과 Level 2 문제를 함께 합산합니다.

### 행동패턴 경고

로그인 이벤트 중 행동패턴 4개 항목 중 하나 이상에 해당된 건수입니다.

이 값은 관리자에게 참고용으로 보여주는 경고입니다. 이 값만으로 사용자를 봇으로 확정하거나 차단하지 않습니다.

### 차단/락아웃

보안 이벤트 중 `blocked` 결과로 저장된 건수입니다.

현재 서비스의 주요 봇 탐지는 문제 풀이 실패나 통과 여부를 중심으로 판단합니다.

## 행동패턴 영역

행동패턴은 점수 없이 4개 항목의 해당 여부만 보여줍니다.

확인하는 항목은 다음과 같습니다.

| 항목 | 의미 |
| --- | --- |
| 궤적 10개 미만 | 로그인 시도 중 수집된 마우스 좌표가 10개 미만인지 확인합니다. |
| 시작점-끝점 기준 직선 이동 | 시작점과 끝점을 잇는 기준선에서 거의 벗어나지 않는 이동인지 확인합니다. |
| 속도 변화율 일정 | 연속 좌표 사이의 속도 변화율이 지나치게 일정한지 확인합니다. |
| 클릭 유지 시간 일정 | 여러 클릭의 down/up 유지 시간이 지나치게 일정한지 확인합니다. |

### 행동패턴 해당 추이

최근 로그인 이벤트 12건을 기준으로 각 시도마다 4개 항목 중 몇 개가 해당됐는지 막대로 보여줍니다.

- 왼쪽은 과거 시도입니다.
- 오른쪽은 최신 시도입니다.
- 값의 범위는 `0~4개`입니다.

### 행동패턴 판정

가장 최근 로그인 시도의 행동패턴 해당 여부를 보여줍니다.

예를 들어 `2/4`라면 4개 항목 중 2개가 해당됐다는 뜻입니다. 이 정보는 관리자 참고용이며, CAPTCHA 문제 풀이 결과를 대체하지 않습니다.

## CAPTCHA 타입별 결과

CAPTCHA 타입별 성공/실패 수를 보여줍니다.

현재 레벨 기준은 다음과 같습니다.

| 레벨 | 타입 |
| --- | --- |
| Level 1 | A, B, C |
| Level 2 | D |

각 타입의 막대는 해당 타입에서 통과한 비율을 시각적으로 보여줍니다.

## 문제풀이 분석

문제풀이 분석은 CAPTCHA 이벤트를 중심으로 사용자의 풀이 결과를 확인하는 영역입니다.

표시하는 주요 값은 다음과 같습니다.

| 값 | 의미 |
| --- | --- |
| 평균 풀이 시간 | 사용자가 답을 제출한 시점의 평균값입니다. |
| 평균 정답 시점 | 문제에 저장된 정답 기준 시점의 평균값입니다. |
| 평균 오차 | 제출 시점과 정답 기준 시점 사이의 평균 절대 오차입니다. |
| 최근 문제 성공률 | 현재 필터링된 최근 문제풀이 기록 중 성공 비율입니다. |

### 최근 문제풀이 기록

개별 CAPTCHA 풀이 시도 목록입니다.

표에는 다음 정보가 표시됩니다.

| 컬럼 | 의미 |
| --- | --- |
| 시도 ID | 보안 이벤트 ID입니다. |
| 사용자 | 해당 시도를 수행한 사용자입니다. |
| 문제 | CAPTCHA 레벨과 타입입니다. |
| 문제 이름 | 실제 출제된 문제 영상 이름입니다. |
| 제출 시간 | 사용자가 답을 제출한 시간입니다. |
| 정답 기준 | 문제에 저장된 정답 시간입니다. |
| 오차 | 제출 시간과 정답 기준의 차이입니다. |
| 결과 | 성공 또는 실패 결과입니다. |
| 시간 | 이벤트가 기록된 시간입니다. |

최근 문제풀이 기록은 자체 검색/필터를 가집니다.

- 문제 이름 검색
- 타입 필터

이 검색 조건은 아래의 문제별 통계 표에는 영향을 주지 않습니다.

### 문제별 통계

문제 이름 기준으로 CAPTCHA 이벤트를 묶어서 통계를 보여줍니다.

표에는 다음 정보가 표시됩니다.

| 컬럼 | 의미 |
| --- | --- |
| 문제 이름 | 실제 출제된 문제 영상 이름입니다. |
| 유형 | CAPTCHA 레벨과 타입입니다. |
| 시도 | 해당 문제의 전체 시도 수입니다. |
| 실패 | 해당 문제의 실패 수입니다. |
| 성공률 | 해당 문제의 성공 비율입니다. |
| 평균 풀이 | 해당 문제의 평균 제출 시간입니다. |
| 평균 오차 | 해당 문제의 평균 절대 오차입니다. |

문제별 통계는 자체 검색/필터/정렬을 가집니다.

- 문제 이름 검색
- 타입 필터
- 정렬

지원하는 정렬 기준은 다음과 같습니다.

- 시도 많은 순
- 통과율 높은 순
- 통과율 낮은 순
- 실패 많은 순
- 풀이시간 긴 순
- 풀이시간 짧은 순
- 오차 큰 순
- 오차 작은 순

## 상세 조회

최근 문제풀이 기록에서 행을 클릭하면 해당 이벤트의 상세 정보를 확인할 수 있습니다.

상세 조회에는 사용자, 결과, 문제 레벨/타입, 문제 이름, 제출 시간, 정답 기준, 오차 등이 표시됩니다.

행동패턴 세부 항목이 있는 이벤트라면 해당 항목도 함께 보여줍니다. 단, 문제풀이 이벤트에서는 행동패턴 정보가 없을 수 있습니다.

## 데이터 저장 기준

대시보드는 `security_events` 테이블을 기준으로 동작합니다.

주요 컬럼은 다음과 같습니다.

| 컬럼 | 용도 |
| --- | --- |
| `phase` | 이벤트 구분입니다. `captcha`, `login` 등이 들어갑니다. |
| `result` | 전체 이벤트 결과입니다. |
| `captcha_type` | CAPTCHA 타입입니다. |
| `captcha_level` | CAPTCHA 레벨입니다. |
| `captcha_name` | 실제 문제 영상 이름입니다. |
| `captcha_result` | CAPTCHA 풀이 결과입니다. |
| `click_time` | 사용자 제출 시간입니다. |
| `badtime` | 문제 정답 기준 시간입니다. |
| `trajectory_points` | 수집된 마우스 좌표 수입니다. |
| `linear_mse` | 직선 이동 여부를 판단하는 선형성 지표입니다. |
| `click_hold_std` | 클릭 유지 시간의 표준편차입니다. |
| `analysis_details` | 행동패턴 4개 항목의 해당 여부 상세 데이터입니다. |

## 운영 해석 기준

- 사용자를 봇으로 확정하는 핵심 기준은 CAPTCHA 문제 풀이 결과입니다.
- 행동패턴은 보조 신호이며, 관리자에게 위험 가능성을 알려주는 참고 정보입니다.
- 문제 이름별 통계를 보면 어떤 문제 영상이 쉽게 통과되는지, 어떤 문제가 많이 실패되는지 확인할 수 있습니다.
- 평균 오차가 낮고 성공률이 높은 문제는 난이도가 낮을 가능성이 있습니다.
- 실패 수가 많고 평균 오차가 큰 문제는 사용자가 많이 막히는 문제일 가능성이 있습니다.
